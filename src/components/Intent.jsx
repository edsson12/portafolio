import React from 'react'


const intent = ({id,titulo,descrip,setEnviarInfo}) => {

    
    setEnviarInfo(id,titulo,descrip)

    
    return (
        <div>
            
        </div>
    )
}

export default intent
